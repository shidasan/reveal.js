// Edit DEOS Script

import("dscript.shell");

boolean killHeavyProcess() {
	int pid = getHeavyProcess();
	String processname = getProcessNameFromPid(pid);
	if(ask "プロセス '${processname}' をkillしてもよろしいですか？") {
		kill -9 ${pid}
	}
}

killHeavyProcess();
